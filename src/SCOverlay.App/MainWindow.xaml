<Window x:Class="SCOverlay.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="SC Overlay"
        MinHeight="360"
        MinWidth="640"
        Height="420"
        Width="760"
        Background="#101418">
    <Grid Margin="28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="16" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="SC Overlay"
                       Foreground="#F2F6FA"
                       FontSize="30"
                       FontWeight="SemiBold" />
            <TextBlock Text="Phase 2 profile/domain foundation"
                       Foreground="#AAB7C4"
                       FontSize="15"
                       Margin="0,6,0,0" />
        </StackPanel>

        <Border Grid.Row="2"
                BorderBrush="#2A3440"
                BorderThickness="1"
                Background="#161D24"
                CornerRadius="6"
                Padding="18">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <TextBlock Text="Foundation status"
                           Foreground="#F2F6FA"
                           FontSize="18"
                           FontWeight="SemiBold" />

                <TextBlock Grid.Row="1"
                           x:Name="StatusText"
                           Foreground="#D5E1EA"
                           FontSize="14"
                           Margin="0,16,0,0"
                           TextWrapping="Wrap" />

                <TextBlock Grid.Row="2"
                           x:Name="DataRootText"
                           Foreground="#9FB0BE"
                           FontSize="13"
                           Margin="0,12,0,0"
                           TextWrapping="Wrap" />

                <TextBlock Grid.Row="3"
                           x:Name="BrowserSourceText"
                           Foreground="#9FB0BE"
                           FontSize="13"
                           Margin="0,8,0,0"
                           TextWrapping="Wrap" />
            </Grid>
        </Border>
    </Grid>
</Window>
